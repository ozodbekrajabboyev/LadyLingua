@props([
    'title' => 'Professional tarjimonlar bilan bog\'laning.',
    'subtitle' => 'Minimalistik tarjima bozori va jamoat markazi. Mutaxassislarni toping, portfoliolarni ko\'rib chiqing va keyingi hamkoringizni yollang.',
    'searchPlaceholder' => 'Til, mutaxassislik yoki tarjimon ismi bo\'yicha qidiring'
])

<section class="w-full max-w-[1200px] px-6 lg:px-40 pt-16 pb-8">
    <div class="max-w-2xl">
        <h2 class="text-[#121117] dark:text-white text-5xl font-black leading-tight tracking-tight mb-4">
            Professional <span class="text-primary">tarjimonlar</span> bilan bog'laning.
        </h2>
        <p class="text-gray-500 dark:text-gray-400 text-lg leading-relaxed">
            {{ $subtitle }}
        </p>
    </div>
</section>

<section class="w-full max-w-[1200px] px-6 lg:px-40 py-6">
    <div class="relative group">
        <div class="absolute inset-y-0 left-4 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-gray-400 group-focus-within:text-primary transition-colors">search</span>
        </div>
        <input class="w-full h-16 pl-14 pr-32 rounded-2xl bg-white dark:bg-gray-900 border-none shadow-sm focus:ring-2 focus:ring-primary/20 text-lg placeholder:text-gray-400 transition-all shadow-[0_4px_20px_-4px_rgba(0,0,0,0.05)]"
               placeholder="{{ $searchPlaceholder }}"
               type="text">
        <div class="absolute inset-y-2 right-2 flex items-center">
            <button class="h-full px-8 bg-primary hover:bg-primary/90 text-white font-bold rounded-xl transition-all shadow-lg shadow-primary/20">
                Qidirish
            </button>
        </div>
    </div>
</section>
