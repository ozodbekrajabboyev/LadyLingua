@props(['label', 'value'])

<div class="flex justify-between items-center">
    <span class="text-xs text-[#656487] dark:text-gray-400">{{ $label }}</span>
    <span class="text-xs font-semibold text-[#121117] dark:text-white">{{ $value }}</span>
</div>
